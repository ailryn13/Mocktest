# ===============================
# ☁️ CLOUD DATABASE (Neon.tech)
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
# Neon requires SSL
spring.datasource.properties.sslmode=require

# ===============================
# ⚡ CLOUD CACHE (Upstash Redis)
# ===============================
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.password=${REDIS_PASSWORD}
spring.data.redis.ssl.enabled=true

# ===============================
# 🛡️ SECURITY & CORS (Vercel)
# ===============================
# Allow your Vercel frontend to talk to Render backend
cors.allowed.origins=${FRONTEND_URL}

# ===============================
# 🤖 JUDGE0 (RapidAPI)
# ===============================
judge0.api.url=https://judge0-ce.p.rapidapi.com
judge0.api.key=${RAPIDAPI_KEY}
judge0.api.host=judge0-ce.p.rapidapi.com
